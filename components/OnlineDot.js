function OnlineDot({ color }) {
  return (
    <div
      style={{
        height: "8px",
        width: "8px",
        borderRadius: "50%",
        backgroundColor: `${color}`,
      }}
    ></div>
  );
}

export default OnlineDot;
